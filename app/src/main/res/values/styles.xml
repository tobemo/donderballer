<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="buttons" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorWhite</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="rankingHeader" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:padding">5dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:background">@color/colorHeader</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="lastGameScore" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:background">@color/colorHeader</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="rankingBody" parent="ThemeOverlay.AppCompat.Light">
        <!--item name="android:padding">5dp</item TODO: mag weg?-->
        <item name="android:textSize">16sp</item>
        <item name="android:background">@color/colorRankingBody</item>
        <item name="android:textColor">@color/colorRankingBody</item>
    </style>
    
    <style name="divider">
        <item name="android:layout_width">2dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

</resources>
